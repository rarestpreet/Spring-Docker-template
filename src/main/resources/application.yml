spring:
  application:
    name: spring-docker-template

  sql:
    init:
      mode: always

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  profiles:
    active: dev

server:
  port: ${PORT:8081}